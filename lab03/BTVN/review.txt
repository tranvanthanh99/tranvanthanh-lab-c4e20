1. Why should we use functions at all?
-> we use functions to reduce the length of our code

2. How to define/declare a function?
-> we use def function(): to define a function

3. How to call/use a function?
-> first, import the fuction then tpye the name of the function to use it

4. What is return, why and how do we use it?
-> the retuen statement is used to return a value to its caller. return[]

5. Do we have to use return in every function?
-> NO

6. What are function arguments/parameters, why and how we use it?
-> an argument of a function is a specific input in the function, also known as an independent variable

7. How to use function from a different file other than our currently working file?
-> 
import sys
sys.path.insert(0, 'path/to/your/py_file')

import py_file